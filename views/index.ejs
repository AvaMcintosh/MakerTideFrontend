<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maker Tide</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
</head>

<body>

    <!-- Include dynamic header partial -->
    <!-- If user is not null include logged in navbar -->
    <!-- Otherwise include generic navbar -->
    
        <% if (user !=null) { %>
            <%- include("partials/userheader") %>
        <% } else { %>
            <%- include("partials/header") %>
                <% } %>

                    <section id="hero">
                        <div class="hero container">
                            <div>
                                <% if (user !=null) { %>
                                    <h1>Welcome back to Maker Tide, <%= user.username %>!</h1>
                                    <% } else { %>
                                        <h1>Welcome to Maker Tide,</h1>
                                        <% } %>

                                            <h3>Have an idea? Maker Tide helps you turn it into reality.</h3>
                                            <h3>Post your custom project, review bids from talented makers, and choose
                                                the perfect match.</h3>
                                            <!-- <a href="/post_a_project" class="cta">Post a project</a> -->
                                            <a class="post" href="post_a_project.html">Post a project</a>
                            </div>
                        </div>
                    </section>

</body>

</html>